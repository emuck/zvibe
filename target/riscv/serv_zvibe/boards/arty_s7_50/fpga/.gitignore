# Vivado session logs (regenerated each session)
vivado.jou
vivado.log
vivado_*.backup.jou
vivado_*.backup.log

# Vivado build outputs (regenerated by make build)
build/

# Flash image / layout (generated by ../sw/Makefile)
*.mcs

# Boot stub hex (copied from ../sw/ by build.tcl at build time)
flash_boot_banner.hex
